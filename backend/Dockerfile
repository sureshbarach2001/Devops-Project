# DockerFile for the backend service
# This file is used to build the backend service image
# The image is built using the node image
# The image is built using the Dockerfile in the backend directory
# The image is built using the npm install file in the backend directory

# Use the node image
FROM node:18
# Set the working directory to /app

# Copy the package.json file to the working directory

# Install the dependencies

# Copy the rest of the files to the working directory

# Expose the port 3000

# Run the app

# This Dockerfile is used to build the backend service image. It uses the node:14 image as
# the base image. It sets the working directory to /app and copies the package.json file to
# the working directory. It then installs the dependencies using npm install. It copies the
# rest of the files to the working directory and exposes the port 3000. Finally, it runs the
# app using npm start.

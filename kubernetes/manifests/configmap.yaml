apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: dev
data:
  APP_ENV: development
  BACKEND_PORT: "8000"
  SOCKET_PORT: "4000"
  MONGO_HOST: "mongodb-service" # Service name for MongoDB